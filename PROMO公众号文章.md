# 今天介绍一个我 vibe coding 的本地工具 AideR

> 我用 AI 一句句"搭"出来的 R 分析工具，最特别的是——它里面的 AI，是真的替我跑 R 的。

---

## 写在前面：什么是 vibe coding

最近圈子里很流行一个词："**vibe coding**"。说白了，就是我脑子里有一个想法，然后**跟 AI 一段段地描述**，AI 负责把代码打出来、把界面搭起来、把报错修掉，我负责"带节奏"——说想要什么、哪里不对、再改。我坐在旁边，多半时候更像一个"产品经理 + 验收人"，而不是一个逐行敲代码的程序员。

这个项目就是这么来的：我想要一个"能把 R 分析做明白"的工具，于是跟 AI 你来我往地，把一个想法一点点变成了今天能用的软件。它叫 **AideR**——一句英文标语：*AI that actually runs your R*，翻译过来就是"**AI 真的跑你的 R**"。

说实话，最初我只是想给自己写个顺手的分析工具，结果越做越觉得"AI 不只会说、而会动手去做"这件事太重要了，值得好好讲讲。

**所以今天这篇，不只是介绍一个工具，更想讲讲"AI 应当怎样真正帮人做分析"这件事。**

---

## 一、先说我遇到的痛点：AI 只会"给建议"

先说一个你一定经历过的场景：

你想让 AI"帮你看个统计"。于是你问它，它开始唰唰给你一段代码，还挺像回事：
```r
t.test(age ~ group, data = dat)
```
然后呢？—— **你得自己复制、粘贴、运行。** 报错？再贴回去，它改，你再跑。画个图？它给你 `boxplot(...)`，你还是得自己来。

碰到要连起来做很多步的分析，就变成一场漫长的"复制-粘贴-报错-再问"拉锯战。一天下来，活没少干，图还是自己画的，AI 更像一个"只会给建议、从不负责落地"的顾问。

我就在想：**能不能反着来？让 AI 不只给代码，而是直接在你的 R 里把分析做完？**

这个念头，就是 AideR 的起点。

---

## 二、AideR 是什么：它不是"写代码的助手"，是"替你动手的分析助手"

AideR 是一个**本地运行**的 R 数据分析工作台。核心目标一句话：**AI 写出来的每一段代码，都会真的在你自己的 R 会话里执行，结果会自动回到 AI 那边，让它据此继续。**

它不是给你一段代码让你自己跑，而是**当场就跑给你看**。

你只要对它说一句："读取数据，做个描述性统计，画个箱线图"。
接下来会发生什么？
- AideR **真的**在你的 R 里新建一个代码块，写上"读数据"的代码，运行；
- 再建一块，写"描述性统计"，运行，得到 `summary` 的结果；
- 再建一块，画箱线图，**图就直接显示在块的下方**；
- 它自己看到了结果，再决定下一步；
- 每一步都在你眼前的**一个个代码块**里，你能看得见、随时改、随时重新跑。

它不是一个和你"聊天"的机器人，而是一个**坐在你旁边、真的动手做**的分析助手。

---

## 三、三个最打动我的地方

### 1. 代码是"块"的，AI 能精准操作每一块

AideR 里，你的代码不是一坨，而是**很多有编号的代码块**，比如 `code1-1`、`code2-3`；还有一个**临时窗**用 `scratch-1`。

你甚至可以直接对 AI 说：
> "把数据标准化写到 **code2-1** 并运行"

它就会真的把标准化代码写进那一块、就在那块里运行、结果就显示在那块下面。它像知道你的"布局"，指哪打哪。

这对做分析太重要了：你不再是面对一整块代码，而是把分析拆成一个个能单独运行、单独查看、单独修改的小单元。

### 2. 所有代码块共享**同一个 R 会话**

这也是我会喜欢它的原因。写分析最烦什么？变量对不上。块 A 里建的 `df`，块 B 里用不了，就得重新定义，处处碰壁。

AideR 里，**所有代码块共用同一个 R 会话**——块 A 定义的变量、跑出来的对象，块 B 直接用。你不用来回复制粘贴，变量一路连贯到底。这就像"把 RStudio 的和 Jupyter 的优点揉在一起"。

### 3. 它真的会"记住"你

每个工程文件夹里有一本真实的 `memory.md`。你可以跟 AI 说：
> "记住：本分析数据按 ≥60 岁分组，结局变量是存活。"

它会把这句话写进 `memory.md`。**下次你打开同一个工程，它还记得。** 而且这本 `memory.md` 是真实文件，跟着你的工程文件夹走，随工程一起备份、迁移。

工程、代码、结果、记忆、技能，全在一个文件夹里——这个"工程化 + 记忆"的组合，让分析不再是提一次问就忘一次的聊天，而是**可以长期经营、积累的作品**。

---

## 四、深入一点：它怎么做到"真的跑你的 R"？

很多人会问：这不就是套了一层壳的 ChatGPT 吗？

其实不是。区别在于一个叫 **agent（智能体）循环** 的机制，通俗讲就是：

> **想 → 写代码 → 运行 → 看结果 → 再想 → 再写 → …… 直到完成**

每个回合，AideR 都把你的 R 会话当作"工具"交给 AI：
- 它想探查变量，就只读地看一眼（不污染你的数据）；
- 它想跑分析，就真的运行一段代码，拿到结果；
- 它甚至能看到你 R 里现在有哪些变量、之前跑过什么。

所以它不是在"猜"，而是在**基于你真实的运行结果**不断推进。这正是和普通问答式 AI 最大的分水岭。
（工程上，它靠 `r-engine` 维护一个常驻的 R 进程，用加密的帧协议来回传结果；前端是 Vue + CodeMirror；后端 Fastify + Socket.IO；AI 走 OpenAI 兼容通道——这条后面细说，不用管术语。）

---

## 五、Vibe coding 出来，功能还挺全

既然聊到项目，我把做出来的东西先摆一摆：

- **多代码窗口 + 智能编辑器**：R 语法高亮、自动补全（常用函数 + 你当前的变量）、一键运行"当前块 / 光标处表达式"，还能识别一行逻辑写成多行的函数调用和 `+`、`%>%` 续行。
- **AI 多步分析 + 计划（Plan）**：勾一下，AI 先给步骤清单，再逐步执行、每步汇报进度——适合复杂长任务。
- **技能（Skill）**：把常用的分析流程做成"模板"，AI 按模板规范化做；没指定时它会自己挑最合适的。你还能往 `skills/` 文件夹里加自己的技能。
- **中 / 英双语**：界面语言和 AI 回复语言**分开切**，互不干扰。
- **本地优先、隐私友好**：默认用本地 AI 模型，**数据不出你机器**——这点对科研/临床数据非常关键。
- **工程 / 记忆 / 技能一体**：一个文件夹管我上面说的所有东西。

这些功能当然不是我一夜写出来的，而是和 AI"带节奏"一点一点磨出来的——哪些用户会用到、哪里该顺、哪里别太复杂。很享受这个过程。

---

## 六、它最拿手的场景

- **临床 / 科研数据**：描述性统计、t 检验、卡方、秩和、线性/逻辑回归（直接给你 OR 和 95% 置信区间）。
- **生信分析**：差异分析、火山图、富集这类流程也能搭。
- **日常数据处理**：导入 Excel / CSV、清洗、画图、写报告。
- **想严格复核每一步**：勾上「需授权运行」，AI 的每段代码都会先停下来等你确认，再运行。

---

## 七、上手其实很简单

装好后三句话就能用起来：
```
npm install
npm start
# 浏览器打开 http://127.0.0.1:8787
```
之后在右下角跟 AI 说第一句话就行。项目里有完整的中文教程（`TUTORIAL.zh-CN.md`），从入门到排错都有。

> 补充一句：AideR 只依赖 **Node.js 和 R**这两个常见环境。若只想手动写码运行不用 AI，把它设为关闭即可，它本身就是个顺手的 R 工作台。

---

## 八、说回 vibe coding 的感想

借这个项目，我最深的体会是：**好的工具，不是堆功能，而是想清楚"该让 AI 动手、还是只让 AI 动嘴"。** 在数据分析这个场景，答案很明确——**让 AI 动手**。它写码、运行、看结果、再继续，把人从"复制粘贴报错"的循环里解放出来，让人专注于"我要分析什么、结果说明了什么"。

我把它开源到了 GitHub（MIT 协议），仓库地址：**https://github.com/suinchn/AideR**。
里面有 README（中英文）、详细教程、以及本文。欢迎感兴趣的朋友去"跑一跑、用一用、提一提意见"——尤其是反馈你的使用体验，好让我知道接下来该往哪个方向打磨。

毕竟，vibe coding 最迷人的地方，就是**想法可以很快变成能用的东西，而且能一直变好。**

---

*AideR 是研究/工作辅助工具，本文不构成任何医疗决策依据。*
