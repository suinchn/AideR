# AideR 详细使用教程（中文）

> 这篇教程带你从打开网页到完成一次完整的 R 分析，尽量覆盖界面、代码块、AI 助手、工程、技能、记忆等常用功能。

---

## 目录
1. [认识界面](#一认识界面)
2. [十分钟跑通第一个分析](#二十-分钟跑通第一个分析)
3. [代码块详解](#三代码块详解)
4. [AI 助手用法](#四ai-助手用法)
5. [工程 / 保存 / 记忆](#五工程--保存--记忆)
6. [技能 Skill 与系统提示词](#六技能-skill-与系统提示词)
7. [常用分析示例](#七常用分析示例)
8. [小技巧与排错](#八小技巧与排错)

---

## 一、认识界面

打开 **http://127.0.0.1:8787** 后，主界面是左右两栏：

**左栏（上方）— 代码区**
- 顶部一排按钮：`清空代码`、`导入 R 脚本`、`导入 ipynb`、`保存 ipynb`、`加代码块`、`▶ 全部运行`、`▶ 运行此处`。
- 下面是**多个代码窗口**（选项卡：Code 1、Code 2…）。每个窗口里有**若干代码块**，编号如 `code1-1`、`code1-2`（临时窗用 `scratch-1`）。
- 每个代码块：
  - 左上角是编号；右上角工具：`▶` 运行本块、`＋` 下方插入、`↑↓` 移动、`🤖` 让 AI 改写本块、`✕` 删除。
  - 中间是代码编辑区，支持 **R 语法高亮 + 自动补全**。
  - 运行后，下方显示**输出/图形/报错**。

**左栏（下方）— Scratch（临时窗）**
- 一块独立的白板，适合临时试验，不占用正式代码窗。

**右栏（上方）— Variables（变量）**
- 显示当前 R 会话里的对象：点击可展开看类型/维度/内容，可直接删单个或清空。

**右栏（下方）— AI Assistant（AI 助手）**
- 底部输入框对 AI 说话；上方有「需授权 / 字号 / AI→中·EN / Plan / Skill / 清空对话」。

**顶部**
- 品牌名 + `New Project / Open / Save`，右侧「设置」「明/暗主题」「界面 中/EN」。
- 底部状态栏显示当前工程名、Rscript 路径、AI 引擎。

---

## 二、十 分钟跑通第一个分析

1. **启动**：按要求装好 Node 和 R 后，`npm start`，浏览器打开上面的地址。
2. **建工程**（可选但推荐）：顶部点 **New Project** → 选一个文件夹当工程（可填完整路径）→ 确定。
   - 工程文件夹里会自动建 `skills/`；之后「保存工程」、AI 记忆 `memory.md` 都在这里。
3. **写第一块代码**：在 Code 1 的第一块里输入：
   ```r
   set.seed(1)
   age <- rnorm(30, mean = 55, sd = 10)
   summary(age)
   ```
4. **运行**：光标放块里按 **Ctrl+Enter**（或点块上的 `▶`），结果会显示在块下方；`age` 会出现在 **Variables** 面板。
5. **画个图**：在下一个代码块里输入 `hist(age, col = "steelblue")` 并运行 —— 图片直接显示在块下方。
6. **问 AI**（可选）：在右下输入 *"对 age 做个描述性统计并解释"* → AI 自己写码、运行、给结论。

> 搞定。你已经完成了一次"建变量 → 统计 → 画图"的完整流程。

---

## 三、代码块详解

### 运行方式（三种）
| 操作 | 作用 |
|---|---|
| 点块上 **`▶`** | 运行**整个代码块** |
| **Ctrl+Enter** 或顶栏 **`▶ 运行此处`** | 运行**光标处的逻辑表达式**：选中了就跑选中部分；没选中就自动扩展成完整表达式（能识别多行函数调用、`+` 和 `%>%` 续行）；跑完光标跳到下一条 |
| 顶栏 **`▶ 全部运行`** | 从上到下依次跑所有块（共享变量，出错会暂停） |

- **所有代码块共用同一个 R 会话**：块 A 定义的变量，块 B 直接能用（这就是 notebook 的核心价值）。
- 想单独改某块 → 点它的 `🤖`，告诉 AI 怎么改；AI 会把改写结果放回该块并可运行。

### 清空 / 增删
- `＋` 在下方插入新块；`✕` 删本块；顶部 `清空代码` 清空整个窗口；`Clear all`（变量面板）清空所有变量。

---

## 四、AI 助手用法

### 基本
在右下输入需求，回车发送。AI 会判断该写什么代码，然后**真的在你的会话里一段段运行**，最后给文字结论。例：
- *"读取所有数据，做描述性统计"*
- *"比较两组能否用 t 检验，画箱线图"*
- *"fit 一个 logistic 回归，给出 OR 和 95%CI"*
### 让它作用于具体代码块
在话里带上块的编号，AI 就写进那一块并运行：
- *"在 code2-1 里写规范化处理并运行"*
- *"把 PCA 画图放到 scratch-2"*
### Plan（分步）
勾选顶部 **Plan**，AI 会先给出 `<r_plan>` 步骤清单，再按步骤执行、每步汇报进度——适合复杂长任务。
### 需授权
默认 AI **自动运行**代码。若想加一道人工审核，开「需授权运行」，之后 AI 每段代码都会先等你点「授权并运行」。
### AI 输出语言
顶部 **AI→中 / AI→EN** 只影响 **AI 说话**的语言，不影响界面。
### 记忆
AI 觉得"以后值得记住"时会写进工程的 `memory.md`。你想主动让它记住，可以说 *"记住：数据截止到 2026-06，按 <60/≥60 岁分组"*。

---

## 五、工程 / 保存 / 记忆

- **New Project**：选（或输入）一个文件夹作为本工程文件夹。
- **Save（保存工程）**：把当前代码块清单 + R 环境快照写入工程文件夹（`project.json` + `<工程名>.RData`）。
- **Open（打开工程）**：载入那个文件夹的工程文件。
- **`memory.md`**：工程里给 AI 跨会话记忆用的真实文件；随工程一起备份/迁移。
- **`skills/`（工程内）**：该工程自带的技能，只对本工程生效。

> 浏览器选文件夹拿不到磁盘绝对路径（安全限制）；想显示确切路径可在新建工程时于「完整路径」框手动填一次。

---

## 六、技能 Skill 与系统提示词

- **技能下拉（AI 窗口）**：预置 `Descriptive statistics`、`Compare two groups`、`Regression / model`、`Write a report` 等。选一个，AI 就按该流程规范化输出。
- **不选** skill：AI 根据任务**自选**最合适的。
- **加自己的技能**：在 `skills/<名字>/skill.md` 写第一行 `# 显示名` + 下面的分析流程；或放在某个工程的 `skills/` 里（仅该工程）。刷新后出现在下拉。
- **系统提示词**（`server/prompt/*.md`）：AI 的整体角色与规则，可手工编辑，改后**重启后端**生效。

---

## 七、常用分析示例

### 1. 导入 Excel / CSV
```r
library(readxl)              # 若无: install.packages("readxl")
dat <- read_excel("data/clinical.xlsx")   # 或 read.csv("data/clinical.csv", check.names=F)
head(dat); str(dat); dim(dat)
```
### 2. 描述性统计 & 分组差异
```r
summary(dat)
library(dplyr)
dat %>% group_by(group) %>% summarise(n=n(), mean=mean(age), sd=sd(age))
t.test(age ~ group, data=dat)      # 正常用 t
wilcox.test(age ~ group, data=dat)  # 偏态用 wilcoxon
chisq.test(table(dat$sex, dat$group))
```
### 3. 线图 / 箱线图 / 柱状图
```r
boxplot(age ~ group, data=dat, col=c("lightblue","salmon"))
barplot(table(dat$sex))
plot(dat$age, dat$bmi, col=as.factor(dat$group))
```
### 4. 线性 / 逻辑回归
```r
m1 <- lm(age ~ bmi + group, data=dat); summary(m1); confint(m1)
m2 <- glm(outcome ~ age + group, data=dat, family="binomial")
exp(coef(m2)); exp(confint(m2))   # OR + 95%CI
```
### 5. 生信：差异表达 / 富集（示意）
```r
# 例如用 limma/edgeR 做差异基因后：
volcano <- data.frame(logFC = sc$logFC, p = sc$P.Value)
plot(sc$logFC, -log10(sc$P.Value), col="grey", pch=20)
```
### 6. 输出结果到工程文件夹
保存工程后，图/表可在工程文件夹里查看；或直接在 R 里 `pdf("figure1.pdf", width=6,height=4); …; dev.off()`。

---

## 八、小技巧与排错

- **共享会话**：一个块报错，Fix 那个块后再跑；前面的变量不受影响（除非报错块有副作用）。
- **Ctrl+Enter 逐条**：光标放某行按 Ctrl+Enter，光标自动跳下一行，适合逐步调试。
- **AI 说"需要 API Key"**：用本地 Ollama 最省心（数据不出机）。
- **AI 说 fetch failed**：Ollama 没启动，或 URL/模型名不对；`ollama serve` 后重试。
- **R 未就绪**：设置里填 Rscript 完整路径（如 `C:/Program Files/R/R-4.4.1/bin/Rscript.exe`）。
- **改动系统提示词没生效**：改的是后端文件，需**重启后端**。
- **想换界面/AI 语言**：界面用顶栏「中/EN」，AI 说话用 AI 窗口「AI→中/EN」，两者独立。

---

*遇到具体问题，把报错原文、你点了哪步、期望结果发给我。*
